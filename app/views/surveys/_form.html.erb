<%= form_with(model: survey) do |form| %>
  <% if survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
        <% survey.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :is_useful %>
    <%= form.check_box :is_useful %>
  </div>

  <div class="field">
    <%= form.label :is_request %>
    <%= form.check_box :is_request %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <td><%= form.select :category_id, @categories %></td>
  </div>

  <div class="field">
    <%= form.label :crop_id %>
    <td><%= form.select :crop_id, @crops %></td>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
